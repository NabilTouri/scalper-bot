[tool.poetry]
name = "scalper-bot"
version = "0.1.0"
description = ""
authors = ["NabilTouri <nabilbrescia00@gmail.com>"]
readme = "README.md"
packages = [
    {include = "scalper_bot"}
]

[tool.poetry.dependencies]
python = "^3.11"
pandas = "^2.2"
numpy = "^1.25"
python-dotenv = "^1.0.0"
alpaca-py = "^0.42.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
